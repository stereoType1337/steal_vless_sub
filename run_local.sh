#!/bin/bash

# Скрипт для запуска бота на localhost

echo "🚀 Запуск vless-bot на localhost..."

# Проверяем наличие .env файла
if [ ! -f .env ]; then
    echo "❌ Файл .env не найден!"
    echo "📝 Создайте файл .env на основе .env.example:"
    echo "   cp .env.example .env"
    echo "   nano .env  # добавьте ваш BOT_TOKEN"
    exit 1
fi

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активируем виртуальное окружение
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Устанавливаем зависимости
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Создаем файл для хранения vless ссылок если его нет
if [ ! -f vless_links.json ]; then
    echo "📄 Создание файла vless_links.json..."
    echo "[]" > vless_links.json
fi

# Запускаем бота в режиме polling
echo "✅ Запуск бота в режиме polling (localhost)..."
python server.py --polling

